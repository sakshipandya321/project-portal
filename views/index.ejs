<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to Project Portal</title>

  <!-- Bootstrap 5 CDN for styling -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Google Fonts for better typography -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">

  <!-- Custom CSS -->
  <style>
    /* Full-page background image */
    body {
      background: url('https://source.unsplash.com/1600x900/?technology,teamwork,project') no-repeat center center fixed;
      background-size: cover;
      font-family: 'Roboto', sans-serif;
      color: white;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    /* Overlay to darken the background for better readability */
    .overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.6);
      z-index: -1;
    }

    .container {
      text-align: center;
      background-color: rgba(0, 0, 0, 0.7); /* Semi-transparent background */
      padding: 40px 60px;
      border-radius: 15px;
      box-shadow: 0px 15px 30px rgba(0, 0, 0, 0.4);
      width: 100%;
      max-width: 500px;
      animation: fadeIn 1s ease-out;
    }

    h2 {
      font-size: 2.5rem;
      margin-bottom: 30px;
      text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.5);
      animation: slideIn 1.5s ease-out;
    }

    p {
      font-size: 1.2rem;
    }

    .btn {
      width: 100%;
      padding: 15px;
      font-size: 1.1rem;
      border-radius: 8px;
      transition: all 0.3s ease;
    }

    .btn-primary {
      background-color: #6a11cb;
      border: none;
    }

    .btn-primary:hover {
      background-color: #2575fc;
      transform: scale(1.1);
    }

    .btn-secondary {
      background-color: #f39c12;
      border: none;
    }

    .btn-secondary:hover {
      background-color: #e67e22;
      transform: scale(1.1);
    }

    .btn:focus {
      outline: none;
      box-shadow: 0 0 15px rgba(255, 255, 255, 0.5);
    }

    footer {
      position: absolute;
      bottom: 10px;
      width: 100%;
      text-align: center;
      color: white;
      font-size: 0.9rem;
    }

    /* Keyframes for text animation */
    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(-30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Fade-in effect for container */
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    /* Floating animation for buttons */
    .btn-primary,
    .btn-secondary {
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-5px);
      }
    }

  </style>
</head>
<body>

  <!-- Dark Overlay -->
  <div class="overlay"></div>

  <!-- Container for the main content -->
  <div class="container">
    <h2>Welcome to the Student, Alumni, and Admin Portal</h2>
    <p class="lead">A platform for managing projects, enhancing communication, and evaluating performance.</p>
    <div class="mt-4">
      <a href="/auth/login" class="btn btn-primary mt-3 mb-3">Login</a>
      <a href="/auth/signup" class="btn btn-secondary mt-3 mb-3">Sign Up</a>
    </div>
  </div>

  <!-- Footer Section -->
  <footer>
    <p>&copy; 2024 Project Portal | All Rights Reserved</p>
  </footer>

  <!-- Bootstrap 5 JS for functionality -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
