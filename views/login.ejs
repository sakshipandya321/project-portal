<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Project Portal</title>
  
  <style>
    /* Global Styles */
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #FFB6C1, #FFE4B5); /* Warm gradient */
      color: #333;
      height: 100vh; /* Ensure body takes full height */
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    h1 {
      text-align: center;
      color: #4F4F4F;
      font-size: 2.5em; /* Reduced font size */
      margin-top: 0;
      animation: fadeIn 2s ease-out;
    }

    header {
      background-color: #FFFFFF;
      padding: 10px 0; /* Reduced padding */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .navbar {
      text-align: center;
      margin-top: 10px; /* Reduced space between navbar and content */
    }

    .navbar a {
      font-size: 1em; /* Reduced font size */
      text-decoration: none;
      color: #6C63FF; /* Soft purple */
      margin: 0 10px; /* Reduced spacing between links */
      padding: 5px 8px;
      border-radius: 5px;
      transition: all 0.3s ease;
    }

    .navbar a:hover {
      background-color: #6C63FF;
      color: #FFFFFF;
      transform: scale(1.05); /* Slightly reduced hover effect */
    }

    .container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100%; /* Make container fill available height */
      flex-direction: column;
      text-align: center;
    }

    .login-form {
      background-color: #FFFFFF;
      padding: 20px 30px; /* Reduced padding */
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
      width: 300px;
      animation: fadeInUp 1.5s ease-out;
    }

    .login-form h2 {
      font-size: 1.8em; /* Reduced font size */
      margin-bottom: 15px; /* Reduced space below header */
      color: #6C63FF;
    }

    .login-form input {
      width: 100%;
      padding: 10px;
      margin: 8px 0; /* Reduced margin between input fields */
      border: 2px solid #DDD;
      border-radius: 5px;
      font-size: 1em;
      transition: border-color 0.3s ease;
    }

    .login-form input:focus {
      border-color: #6C63FF;
      outline: none;
    }

    .login-form input[type="submit"] {
      background-color: #6C63FF;
      color: white;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .login-form input[type="submit"]:hover {
      background-color: #574AE2;
    }

    footer {
      text-align: center;
      margin-top: 20px;
      color: #888;
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>

  <header>
    <h1>Project Portal</h1>
  </header>

  <nav class="navbar">
    <a href="/auth/signup">Signup</a>
  </nav>

  <div class="container">
    <% if (error_msg) { %>
      <div class="alert alert-danger"><%= error_msg %></div>
    <% } %>
    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form action="/auth/login" method="POST" class="login-form">
      <h2>Login</h2>
      <input type="text" name="username" placeholder="Username" required>
      <input type="password" name="password" placeholder="Password" required>
      <input type="submit" value="Login">
    </form>
  </div>

  <footer>
  </footer>

</body>
</html>
